[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npx pnpm@10.11.0 install --frozen-lockfile"]

[phases.build]
cmds = ["npx pnpm@10.11.0 --filter api build"]

[start]
cmd = "npx pnpm@10.11.0 --filter api start:prod"
