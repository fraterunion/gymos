[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm install -g pnpm@10.11.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter api build"]

[start]
cmd = "pnpm --filter api start:prod"
